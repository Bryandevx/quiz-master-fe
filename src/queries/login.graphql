mutation login($data: LoginUserInput!) {
  login(data: $data) {
    access_token
    expiresAt
    user {
      id
      uuid
      email
      username
      type
      lastName
      createdAt
      updatedAt
    }
  }
}
